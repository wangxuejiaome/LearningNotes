<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wxj.interview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activitychapter.ESingleTaskActivity"></activity>
        <activity
            android:name=".activitychapter.DSingleTaskActivity"
            android:launchMode="singleTask"
            android:taskAffinity="com.wxj.interview1" />
        <activity
            android:name=".activitychapter.CSingleTaskActivity"
            android:launchMode="singleTask"
            android:taskAffinity="com.wxj.interview1" />
        <activity android:name=".activitychapter.BActivity" />
        <activity
            android:name=".activitychapter.AActivity"
            android:configChanges="orientation" />
        <activity android:name=".activitychapter.ActivityChapterHomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>